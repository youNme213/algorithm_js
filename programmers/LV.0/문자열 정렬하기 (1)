function solution(my_string) {
    const numbers = my_string
                            .match(/\d/g)
                            .map(Number)
                            .sort((a, b) => a - b);
    return numbers
}
