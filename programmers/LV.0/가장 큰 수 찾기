function solution(array) {
    const newArr = [...array].sort((a, b) => a-b);
    const biggestNum = newArr.at(-1);
    const arrayIndex = array.indexOf(biggestNum);

    return [biggestNum, arrayIndex];
}
