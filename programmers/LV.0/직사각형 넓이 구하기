function solution(dots) {
    const xDots = dots.map(dot => dot[0]);
    const yDots = dots.map(dot => dot[1]);
    
    const width =  Math.max(...xDots) - Math.min(...xDots);
    let height =  Math.max(...yDots) - Math.min(...yDots);
    
    return width * height;
}
